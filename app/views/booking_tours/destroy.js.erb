<%= sanitize custom_bootstrap_flash %>

<% if flash[:success].present? %>
  $("#booking_tours")
    .html("<%= j render partial: 'booking_tours/booking_tour', collection: @booking_tours %>")
<% end %>
